# ComplianceEase — Environment Variables
# Copy this file to .env and fill in your values
# NEVER commit .env to git

# ── SUPABASE ─────────────────────────────────────────────────────────
SUPABASE_URL=https://xxxx.supabase.co
SUPABASE_ANON_KEY=eyJ...          # safe to expose to frontend
SUPABASE_SERVICE_ROLE_KEY=eyJ...  # BACKEND ONLY - never expose

# ── XERO ─────────────────────────────────────────────────────────────
# Apply at: https://developer.xero.com/myapps
# Required scopes: accounting.reports.read, accounting.settings, offline_access
XERO_CLIENT_ID=your_xero_client_id
XERO_CLIENT_SECRET=your_xero_client_secret   # BACKEND ONLY
XERO_REDIRECT_URI=https://yourdomain.com/auth/xero/callback.html

# ── MYOB ─────────────────────────────────────────────────────────────
# Apply at: https://developer.myob.com
# Create a "Desktop & Web" application
MYOB_CLIENT_ID=your_myob_client_id
MYOB_CLIENT_SECRET=your_myob_client_secret   # BACKEND ONLY
MYOB_REDIRECT_URI=https://yourdomain.com/auth/myob/callback.html

# ── XORO ─────────────────────────────────────────────────────────────
# Coming in v2.1 — check https://xoro.com for API access
XORO_API_KEY=your_xoro_api_key
XORO_REDIRECT_URI=https://yourdomain.com/auth/xoro/callback.html

# ── STRIPE ───────────────────────────────────────────────────────────
# From: https://dashboard.stripe.com/apikeys
STRIPE_PUBLISHABLE_KEY=pk_live_...  # safe to expose to frontend
STRIPE_SECRET_KEY=sk_live_...       # BACKEND ONLY
STRIPE_WEBHOOK_SECRET=whsec_...     # BACKEND ONLY

# ── RESEND (Email reminders) ──────────────────────────────────────────
# From: https://resend.com
# Free tier: 3,000 emails/month
RESEND_API_KEY=re_...

# ── TWILIO (SMS reminders) ────────────────────────────────────────────
# From: https://console.twilio.com
# Buy an Australian mobile number (~$1.50/month)
TWILIO_ACCOUNT_SID=AC...
TWILIO_AUTH_TOKEN=...               # BACKEND ONLY
TWILIO_FROM_NUMBER=+61...           # Your Twilio AU number

# ── ENCRYPTION ────────────────────────────────────────────────────────
# Used to encrypt OAuth tokens at rest
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
TOKEN_ENCRYPTION_KEY=your_32_byte_hex_string_here

# ── ANTHROPIC (AI Email Composer) ─────────────────────────────────────
# From: https://console.anthropic.com
# Cost: ~$0.003–0.008 per email at Haiku pricing. ~$1.50/month at 200 emails.
ANTHROPIC_API_KEY=sk-ant-...

# ── APP ────────────────────────────────────────────────────────────────
FRONTEND_URL=https://yourdomain.com
PORT=3001
